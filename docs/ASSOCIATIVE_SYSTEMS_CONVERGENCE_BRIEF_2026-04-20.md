# Associative Systems Convergence Brief 2026-04-20

Repo: `D:\End-Game\champion_councl`

Purpose:

- align the current continuity, orientation, attention, routing, and procedural-generation language to what is actually present in local source
- separate confirmed runtime surfaces from doctrine and from still-proposed coupling
- define one careful next-sprint sequence so Pan/Tinkerbell/procgen work lands into the existing substrate instead of fragmenting into parallel metaphors

Primary grounding:

- source:
  - `continuity_restore.py`
  - `server.py`
  - `scripts/text_theater.py`
  - `static/main.js`
- local docs:
  - `docs/OPERATIVE_MEMORY_ALIGNMENT_SPEC_2026-04-15.md`
  - `docs/QUERY_MIRROR_UNIFICATION_SITREP_2026-04-15.md`
  - `docs/PAN_SUPPORT_FIELD_PROCGEN_SPEC_2026-04-15.md`
  - `docs/PAN_TINKERBELL_EMBODIED_AUTONOMY_POSITIONING_2026-04-10.md`
  - `docs/PHYSICAL_DIAGNOSTIC_FACILITY_OF_FACILITIES_BLUEPRINT_2026-04-18.md`
  - `docs/TINKERBELL_HOLD_SHUTTER_DREAMER_CODE_ALIGNMENT_CHECKPOINT_2026-04-19.md`
  - `docs/CONTINUITY_NAMING_DEPRECATION_RECORD_2026-04-20.md`

## Bottom Line

The correct local read is:

- `Tinkerbell` and `Pan` are not literally the same system
- they are already converging as a conjunctive unit over one orienting substrate
- `Tinkerbell` is the attention/prospect lane
- `Pan` is the support/contact/route measurement lane that can grow into proposal-routing
- `query_thread`, `output_state`, `equilibrium`, and continuity packet fields are the current carrying surfaces that keep those lanes aligned
- Coquina, palette evolution, breadcrumb-style sequence carrying, Dreamer, and HOLD should consume those carrying surfaces instead of inventing a second authority plane

That means the right convergence target is:

- one orientation stack
- one evidence order
- one derived orienting surface
- multiple bounded facilities over that same truth

Not:

- one merged god-system
- one new metaphor per lane
- one hidden continuity runtime

## Confirmed Runtime Surfaces

### 1. Continuity is already packetized

Confirmed in `continuity_restore.py`.

The archive-side packet already carries:

- `query_state`
- `resume_focus`
- `surface_prime`
- `reset_boundary`
- `paired_state_resource`

These are the actual continuity-side carriers.

### 2. The live orienting surface is already real

Confirmed in `static/main.js`, `server.py`, and `scripts/text_theater.py`.

The current live carrier is `output_state`, with child blocks including:

- `placement`
- `trajectory_correlator`
- `continuity_cue`
- `tinkerbell_attention`
- `field_disposition`
- `watch_board`
- `equilibrium`
- `drift`
- `next_reads`
- `receipts`
- `freshness`
- `confidence`
- `sources`
- `pan_probe`

This is the crane the other systems should read from.

### 3. Blackboard/query-work is the visible sequence carrier

Confirmed in `shared_state.blackboard.working_set.query_thread` consumers across `server.py`, `static/main.js`, and `scripts/text_theater.py`.

The live sequence carrier already exposes:

- `sequence_id`
- `segment_id`
- `session_id`
- `subject_key`
- `current_pivot_id`
- `objective_id`
- `objective_label`
- `visible_read`
- `anchor_row_ids`
- `priority_pivots`
- `help_lane`
- `next_reads`
- `raw_state_guardrail`

This is already close to the breadcrumb/root-sequence lane people keep describing.

### 4. Tinkerbell is live as a pointer lane

Confirmed in `static/main.js` `_envBuildTinkerbellAttention(...)` and in the downstream consumers.

Current live shape:

- `band`
- `summary`
- `attention_kind`
- `attention_target`
- `attention_confidence`
- `hold_candidate`
- `active_pointer`
- `prospect_candidates`

Current truth:

- pointer-only
- derived from current orienting state
- visible in blackboard/text-theater/env-report/Dreamer context
- not a second authority plane

### 5. Pan is partly real through `pan_probe`

Confirmed in `static/main.js`, `server.py`, and `scripts/text_theater.py`.

Current live shape already exposes:

- selected contact joint / bone
- contact state
- support role
- contact bias
- grounding/alignment values
- support phase
- timeline sample
- support surface
- writer identity
- association surfaces
- capture surfaces

Current truth:

- this is a measurement/probe lane now
- it is not yet full Pan autonomy
- it is already a valid docking point for support-field and routing work

### 6. Capture-side shutter vocabulary is real

Confirmed in `static/main.js`.

Valid current capture-side terms include:

- `live_render_shutter`
- `structured_snapshot_shutter`
- `contact_body_shutter`
- `web_theater_shutter`

These belong to the capture/corroboration lane, not the continuity architecture.

## Confirmed Doctrine, Not Yet Fully Landed Runtime

### 1. Pan support-field procgen

Confirmed as the declared pivot and documented next slice.

Current correct read:

- proposal-first
- support substrate may compensate toward intended body demand
- still bounded by contact/balance truth
- still must not bypass `workbench_stage_contact`

### 2. Dreamer as the bounded proposal lane

Confirmed in source as a consumer of query-thread and output-state context.

Current gap:

- Dreamer sees meaningful orienting state
- Dreamer does not yet directly score from Tinkerbell pointer fields as a first-class feature

### 3. Breadcrumb-style sequence carrying

Partly confirmed.

What is real now:

- query-thread identity
- continuity packet identity
- return-path lanes
- reset-boundary doctrine

What is not yet first-class:

- a named breadcrumb packet with explicit replay/version semantics

### 4. Coquina and palette evolution

Confirmed as real doctrine and data-contract work.

Current correct read:

- Coquina is a procedural generation family
- palette evolution is a real substrate family
- neither should become an independent continuity or orientation authority
- both should consume the current orientation and receipt surfaces when they are operationalized

## Corrections To The Current Mental Model

### `Tinkerbell = Pan`

Not supported literally.

Better reading:

- `Tinkerbell` points
- `Pan` measures/routes/proposes
- together they form one conjunctive orientation-to-routing unit

### `adrenaline` as the active continuity runtime

Not supported.

The active architecture now uses:

- `continuity_restore`
- `query_state`
- `resume_focus`
- `surface_prime`
- `reset_boundary`
- `query_thread`
- `output_state`
- `equilibrium`

### abstract continuity-side `shutter`

Not supported.

Only capture-side shutter vocabulary remains active.

## Working Integration Model

The clean integration stack is:

1. `continuity_restore` recovers archive posture
2. fresh theater/capture/blackboard/snapshot reads re-authorize the live seam
3. `query_thread` carries the finite sequence
4. `output_state` carries placement, drift, equilibrium, and next-read posture
5. `Tinkerbell` points at the hottest seam
6. `Pan` measures and later routes/proposes across support truth
7. Dreamer scores bounded proposals
8. HOLD latches human intervention where needed
9. Coquina / palette / procedural families consume the same orienting surface instead of bypassing it

## Next Honest Build Order

### 1. Finish doctrinal alignment first

Update the active trajectory and operative-memory docs so resets stop re-litigating:

- whether Tinkerbell and Pan are separate
- whether adrenaline/shutter are still active continuity names
- whether Coquina/palette/breadcrumb are authorities or consumers

### 2. Promote one compact associative systems map

Future docs should speak in this order:

- continuity packet
- blackboard/query-thread
- output_state/equilibrium
- Tinkerbell pointer
- Pan probe/support-field proposal
- Dreamer/HOLD
- Coquina/palette consumers

### 3. Add one explicit bounded packet for the missing middle

The most useful next packet is not a giant new runtime.

It is a small bounded carrier that can connect:

- current query identity
- current Tinkerbell pointer
- current Pan/support seam
- current capture boundary
- next proposal target

### 4. Keep Pan proposal-first

Do not jump to autonomous routing.

First make Pan capable of:

- support-field proposal receipts
- explicit route/support candidate packets
- inspectable acceptance/blocker reasons

### 5. Let Coquina and palette consume the same sequence

When the procedural families re-enter the hot lane, they should read:

- `query_thread`
- `output_state`
- `tinkerbell_attention`
- `pan_probe`
- receipts and capture boundaries

They should not invent:

- a second continuity stack
- a second palette authority
- a second route board

## Guard Rails

- No second truth plane.
- No literal merge of Tinkerbell and Pan.
- No reactivation of `adrenaline` or abstract continuity-side `shutter`.
- No Pan bypass around support/contact/balance truth.
- No Coquina/palette/or procedural family allowed to bypass `query_thread` and `output_state` once they dock to the active lane.
- No new metaphor should be promoted until it is tied to an existing packet or field in source.

## Current Sprint Read

The current sprint should be treated as:

- an associative alignment sprint
- a doc and memory tightening pass
- a packetization pass over already-real surfaces

Not yet:

- a 3D content sprint
- a full Pan autonomy sprint
- a Pi-provider sprint

Those later goals become safer once the carrying surfaces above are aligned and named consistently.
