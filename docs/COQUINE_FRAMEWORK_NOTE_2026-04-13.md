# Coquine Framework Note 2026-04-13

Purpose:

- park the parent-framework idea cleanly so it survives resets
- keep it available for future proc-gen / generative-system work
- avoid letting it hijack the active local Champion Council trajectory

## Naming

Preferred name for the broader parent framework:

- `coquine system`

This is the current label for the upstream quine/speciation/lattice direction.

It is not a rename of Champion Council itself.

## What It Refers To

The `coquine system` is the broader framework direction implied by the upstream primitives in:

- `F:\End-Game\ouroboros-key\agent_compiler.py`
- `F:\End-Game\ouroboros-key\speciation.py`
- `F:\End-Game\ouroboros-key\swarm_lattice.py`

Champion Council is one descendant/runtime/consumer built around one generated capsule artifact from that broader family.

## Correct Frame

The `coquine system` should be understood as a reusable framework for proc-gen and state-space systems.

It should eventually provide generic primitives for:

1. substrate registration
2. capsule compilation / sealing
3. replication / mutation / branching
4. speciation / partitioning
5. provenance / lineage lattice
6. convergence / selection
7. exact replay by lineage/hash

The host system should define:

- what varies
- what runs
- how outcomes are judged

The framework should provide:

- sealed runnable artifacts
- branching and replay
- lineage and provenance
- evolutionary control surfaces

## Strategy Distinction

`sealed speculative fan-out` / `octopus opens the jar` remains:

- one strategy layer over `coquine`
- not the whole substrate

Do not collapse `coquine` into:

- a batch processor
- a workflow engine
- one fan-out policy
- Champion Council alone

## Operational Rule For This Repo

Inside `champion_councl`:

- treat `coquine` as documented parent-horizon doctrine
- do not let it derail the active local runtime trajectory unless the user explicitly pivots upward

Current local priority has moved since this note was first written.

Current 2026-04-14 read:

1. blackboard/query-work deepening as the orienting surface
2. weather/text-theater/web-theater continuity as the active proving ground
3. relay/parity bottlenecks only where they directly block truthful paired reads
4. measurement/articulation expansion after the orienting/query contract is sharper

So inside `champion_councl`:

- keep `coquine` parked as the parent-horizon doctrine
- but dock any local breadcrumb/query/control ideas to the blackboard/query-work lane first

## Reset Use

If a future session touches:

- proc-gen systems
- generative systems
- upstream quine framework work
- reusable evolutionary substrate work

then read this note together with:

- `docs/OPUS_SUBSTRATE_FIRST_ACCLIMATION_2026-03-29.md`
- `docs/CODEX_CHAMPION_COUNCIL_INSTANTIATION_PROMPT_2026-04-13.md`
- `C:\Users\Jeff Towers\.codex\memories\champion-council-quine-framework-correction-2026-04-13.md`
