# Operative Memory Alignment Spec 2026-04-15

Repo: `D:\End-Game\champion_councl`

Purpose:

- make the agent's reacclimation and working posture a first-class runtime concern
- keep associative surfaces aligned after context compression
- reduce transcript archaeology by moving operational memory into the existing continuity/query/theater/report lane

## Bottom Line

Operational memory alignment is now the top pivot.

This does not mean inventing a second memory system.
It means carrying the current working orientation through the existing surfaces so a compressed restart can recover:

- the active pivot stack
- the active sequence
- the current objective and subject
- the next valid corroboration order
- the recommended docs
- the reset boundary

## Why It Comes First

Without this layer, every later system lands into avoidable reacclimation loss:

- Pan support-field work
- archived/live paired-state work
- query/mirror unification
- breadcrumb and coquine-adjacent sequencing

If the agent cannot recover its own operational stance quickly and truthfully, every downstream system pays a startup tax.

## Existing Docking Surfaces

This spec does not authorize a new authority plane.

It docks to:

- `continuity_restore`
- blackboard query thread
- text theater consult surfaces
- `env_report`
- `env_help`
- repo doctrine docs

Current convergence note:

- the alignment payload is the carrying surface for the Pan/Tinkerbell/procgen sprint
- `Tinkerbell` should consume the payload as the attention/prospect lane
- `Pan` should consume the payload as the support/contact/route measurement and later proposal lane
- breadcrumb-style sequence carrying should be treated as a refinement of this payload, not a competing mechanism
- Coquina and palette/procedural families should consume the same payload when they dock to the active lane

These are the places the alignment should be carried.

## Minimum Alignment Payload

The minimum operative memory alignment payload is:

- `current_pivot_id`
- `priority_pivots`
- `sequence_id`
- `segment_id`
- `session_id`
- `subject_key`
- `objective_id`
- `help_lane`
- `next_reads`
- `recommended_docs`
- reset-boundary fields

That is enough to recover stance without pretending to recover hidden reasoning.

## Future Direction

Later breadcrumb/reset-boundary/coquine logic should consume this payload rather than bypass it.

The eventual goal is:

- the system stops manually rereading whole doctrine sets on every compression
- the reacclimation surfaces become the natural ushering lane
- manual rereading remains the fallback, not the default survival strategy

The current correct reading is:

- `query_thread` carries the finite sequence
- `output_state` carries the derived orienting surface
- `Tinkerbell` points at the hottest seam
- `Pan` measures that seam now and later routes/proposes over truthful support state
- continuity packets keep those lanes re-enterable after reset

## Local Rule

When a new pivotal declaration lands, operative memory alignment must absorb it immediately:

- registry doc
- continuity packet
- blackboard query thread
- text theater consult
- env_report session thread

If a pivot is not present there, it is not yet operationally aligned.
