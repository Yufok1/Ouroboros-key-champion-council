# Python/runtime
.venv/
__pycache__/
*.pyc
*.pyo
.env
.env.*

# Root runtime artifacts
data/
exports/
capsule_state_*.pt
capsule/.bag_state.json
capsule/.mcp_server.log
capsule/.relay_center.cmd
capsule/*.bak_*
capsule/champion_gen8.py
capsule/champion_gen8_bag_of_tricks/
capsule/quine_*.py

# Self-deploy runtime artifacts
self_deploy/START_DOCKER.bat
self_deploy/capsule/*
!self_deploy/capsule/.gitkeep
self_deploy/capsule_state_*.pt
self_deploy/data/
AGENT_ONBOARDING.md

# Keep self-deploy tree out of the private Space repo
self_deploy/

# Claude Code local state
.claude/

# Capsule ephemeral
capsule/.asset_cache/
bag_export.json

# Spawned quine children
children/

# One-off scripts (ingestion helpers already ran)
scripts/
!scripts/
scripts/*
!scripts/sync-pack-dataset.py
!scripts/build-recast-nav.mjs
!scripts/recast-nav-browser-entry.mjs

# JS local dependencies
node_modules/

# Diagram scratch
merril.d2/

# Runtime capture outputs
static/captures/

# HF upload resume metadata for large pack syncs
static/assets/**/.cache/

# Public repo staging lives as its own nested Git repo
docs/brotology/public_repo_root/
docs/brotology/audible..istaken/

# Local sidecar experiments live outside the tracked Council payload
Holo-Ghost/
model_repos/

# Local NotebookLM podcast exports in the Brotology lane
docs/brotology/*.m4a
docs/brotology/*.mp4
docs/brotology/*.zip
