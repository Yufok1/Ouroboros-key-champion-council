# SITREP — Ouroboros MCP + Project Wrappers

Date: 2026-02-25

## 1) MCP Operational Snapshot

- MCP server reachable: `champion-ouroboros-space` (147 tools visible)
- Capsule status: generation 8, quine hash `41fc13b3da8ef80391b921d8e97a10de`
- Council slots: 32 total, 4 plugged
  - `BAAI/bge-small-en-v1.5` (embedder)
  - `Qwen/Qwen2.5-0.5B-Instruct` (LLM)
  - `BAAI/bge-reranker-base` (CrossEncoder)
  - `X1AOX1A/WorldModel-Webshop-Qwen2.5-7B`
- Consensus: bayesian
- Integrity/hash verification: valid
- Frozen state: false

## 2) FelixBag Snapshot

- Total items: 50
- Total size: ~35.97 MB
- Largest item: `quine_source` (~34.8 MB) — intentionally not expanded in this evaluation
- Types observed: document, config, workflow, workflow_execution, state, model_artifact, text, pocket, slot_backup

### High-value bag docs found
- `workflow_automation_guide`
- `web_search_workflow_guide`
- `mcp_integration_guide`
- `cascade_tools_guide`
- `remote_gpu_executor_guide`
- `vast_gpu_rental_guide`
- `arcade_integration_guide`

### Active workflow assets in bag
- `workflow:deep-swarm`
- `workflow:entropy-loop`
- `workflow:health_check_pipeline`
- `workflow:parallel_embed_test`
- 3 execution artifacts captured and queryable

## 3) Wrapper Architecture Assessment

### VS Code Wrapper (root project)
- TypeScript extension host + integrated webview panel
- Local MCP manager with SSE reconnect + activity ingestion
- Runtime category toggles and diagnostics wiring
- Nostr + voice + marketplace integration in extension context

### HF Space Wrapper (`Champion_Council`)
- FastAPI server + capsule subprocess + static web panel
- MCP SDK client for local proxying and reverse-proxy endpoints (`/mcp/sse`, `/mcp/messages`)
- Activity SSE stream for live feed
- Dreamer and Vast aggregated routes implemented
- Browser shim (`vscode-shim.js`) emulates VS Code messaging bridge

### Drift signals noted
- Wrapper tool/category metadata has stale aliases in places (`crystallize`, `resume_agent`, `council_broadcast`) vs current tool names (`implode`, `defrost`, `broadcast`).
- Space and extension UI scripts are similar but not identical, increasing maintenance cost.

## 4) Standalone Readiness

A dedicated workspace has been created:
- `Champion_Council/self_deploy/`
- includes initial docs, architecture notes, phased plan, and `.env.example` template.

## 5) Immediate Priority

Build a unified local runtime skeleton in `self_deploy/`:
1. backend launcher + MCP proxy
2. panel baseline import
3. secrets/integration status service
4. parity hardening for Dreamer/Vast/workflows
